class Item < ActiveRecord::Base
  # def initialize(filtered_data)
    # filtered_data = result.slice(:id, :title, :seller, :price, :stop_time, :permalink, :address,
    #   :seller_address, :seller_contact)
    # @filtered_data = result.slice(:id, :title, :price, :stop_time, :permalink)
  # end
end
